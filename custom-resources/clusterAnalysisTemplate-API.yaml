apiVersion: argoproj.io/v1alpha1
kind: ClusterAnalysisTemplate
metadata:
  name: api-respond
spec:
  args:
  - name: service-name
  metrics:
  - name: api-respond
    interval: 10s
    count: 20
    failureLimit: 3
    provider:
      job:
        spec:
          ttlSecondsAfterFinished: 600
          activeDeadlineSeconds: 220
          backoffLimit: 2
          template:
            spec:
              containers:
              - name: curl-api-test
                image: curlimages/curl:latest
                args: ["-fksSLIw", "%{http_code}", "{{args.service-name}}", "-o", "/dev/null"]
              restartPolicy: OnFailure